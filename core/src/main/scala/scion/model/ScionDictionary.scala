package scion.model

object ScionDictionary {
  val prefix: String = "scion:"
  val evalKey: String = prefix + "eval"
  val tagKey: String = prefix + "tag"
  val importKey: String = prefix + "import"
}
